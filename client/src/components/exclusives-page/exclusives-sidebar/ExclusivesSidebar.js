import React, { } from 'react';

import style from './exclusives-sidebar.module.scss';

// *************************** EXCLUSIVES SIDEBAR COMPONENT *************************** //
const ExclusivesSidebar = () => {
  return (
    <nav className={style.exclusivesSidebar}>
      SIDEBAR
    </nav>
  )
};

export default ExclusivesSidebar;